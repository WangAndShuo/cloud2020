<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.springcloud.dao.PaymentDao">

    <!--   主要是在主键是自增的情况下，添加成功后可以直接使用主键值，其中keyProperty的值是对象的属性值不是数据库表中的字段名-->
    <insert id="create" parameterType="com.atguigu.springcloud.entities.Payment" useGeneratedKeys="true" keyProperty="id">
        insert into db2019.payment(serial) values(#{serial});
    </insert>

    <resultMap id="BaseResultMap" type="com.atguigu.springcloud.entities.Payment">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="serial" property="serial" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="getPaymentById" parameterType="long" resultMap="BaseResultMap">
        select * from payment where id=#{id};
    </select>

    <select id="findParamList"  resultType="com.atguigu.springcloud.domain.UserInfo" parameterType="com.atguigu.springcloud.domain.UserInfo">
       select * from person  limit #{pageNum}, #{pageSize}
    </select>



    <select id="findParamListNum"  resultType="int" >
        select count(*) from person
    </select>
    <!--<select id="getPaymentById" resultMap="BaseResultMap">-->
    <!--select count(*) from payment -->
    <!--<where>-->
    <!---->
    <!--<if test="id != null || id != ''">-->
    <!--<foreach collection="list"  item="set" index="index" open="(" separator="," close=")" >-->

    <!--</foreach>-->
    <!--</if>-->
    <!--</where>-->
    <!---->
    <!---->
    <!--</select>-->
    <!--    <insert id="create" parameterType="Payment" useGeneratedKeys="true" keyProperty="id">-->
    <!--        insert into payment(serial) values(#{serial});-->
    <!--    </insert>-->

    <!--    <resultMap id="BaseResultMap" type="com.eiletxie.springcloud.entities.Payment">-->
    <!--        <id column="id" property="id" jdbcType="BIGINT" />-->
    <!--        <id column="serial" property="serial" jdbcType="VARCHAR"/>-->
    <!--    </resultMap>-->

    <!--    <select id="getPaymentById" parameterType="Long" resultMap="BaseResultMap">-->
    <!--        select * from payment where id=#{id};-->
    <!--    </select>-->
</mapper>